// src/features/clients/index.ts — public API for the clients feature.
// Import from this barrel (deep imports into the feature are lint-forbidden).
export { ClientsPage } from "./ClientsPage";
export { ClientDetailPage } from "./ClientDetailPage";
